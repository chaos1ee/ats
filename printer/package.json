{
  "name": "printer",
  "version": "1.0.0",
  "description": "",
  "bin": {
    "main": "index.js"
  },
  "scripts": {
    "build": "rm -rf build && npx pkg --out-path build . && cp -r ./node_modules/puppeteer ./build/puppeteer"
  },
  "author": "chaos1ee",
  "license": "MIT",
  "dependencies": {
    "path": "^0.12.7",
    "puppeteer": "^2.0.0",
    "shelljs": "^0.8.3",
    "yargs": "^15.0.2"
  }
}
